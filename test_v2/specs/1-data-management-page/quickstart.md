# 快速啟動指南

**功能**: 資料管理網頁

本專案為純前端應用，使用 HTML、CSS 和原生 JavaScript 建置，不需要任何建置流程或 Web 伺服器。

## 如何執行

1.  確保您位於功能分支 `1-data-management-page` 上。
2.  直接在現代的網頁瀏覽器（如 Chrome、Firefox 或 Edge）中開啟 `index.html` 檔案。

這樣就可以了。應用程式將會開始執行，您可以與之互動。所有資料將會儲存在您瀏覽器的 `localStorage` 中。